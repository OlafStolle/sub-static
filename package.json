{
  "scripts": {
    "serve": "cross-env NODE_OPTIONS=--max-old-space-size=8192 browser-sync start --server --files \"**/*.html, **/*.css, **/*.js\""
  },
  "dependencies": {
    "@turf/turf": "^7.2.0",
    "browser-sync": "^3.0.3",
    "cross-env": "^7.0.3"
  }
}
